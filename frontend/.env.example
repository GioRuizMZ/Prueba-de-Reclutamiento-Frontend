# URL base de NUESTRO backend (BFF).
# En local por defecto es http://localhost:3000 (si se omite, se usa ese valor).
# En producción: la URL del backend desplegado en Railway.
VITE_API_URL="http://localhost:3000"
